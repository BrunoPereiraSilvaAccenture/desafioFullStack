import { FornecedoresCepPipe } from './fornecedores-cep.pipe';

describe('FornecedoresCepPipe', () => {
  it('create an instance', () => {
    const pipe = new FornecedoresCepPipe();
    expect(pipe).toBeTruthy();
  });
});
