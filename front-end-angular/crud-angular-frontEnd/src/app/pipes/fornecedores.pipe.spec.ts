import { FornecedoresPipe } from './fornecedores.pipe';

describe('FornecedoresPipe', () => {
  it('create an instance', () => {
    const pipe = new FornecedoresPipe();
    expect(pipe).toBeTruthy();
  });
});
